这个包中预想是存放不同表单关联类型的流程处理器（目前只有外置表单的方式）。
在Activiti中，有三种流程与表单的关联方式：
1、普通表单（固定的表单+固定的存储方式），该种方式是将数据、表单全部交由开发者处理（该方式必须针对不同的业务编写不同的service）
2、外置表单（固定的表单），该种方式将表单交由开发者处理，数据由Activiti进行处理（如果数据需特殊处理，则需要额外保存）
3、动态表单，该种方式下，表单和数据全部交由Activiti处理